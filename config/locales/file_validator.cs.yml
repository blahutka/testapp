# config/locales/en.yml
cs:
  errors:
    messages:
      wrong_size: "is the wrong size (should be %{file_size})"
      size_too_small: "is too small (should be at least %{file_size})"
      size_too_big: "příliš velký soubor (maximálně povolená velikost: %{file_size})"
